---
import { Head } from 'astro-capo';
import BaseHead from '../components/BaseHead.astro';
import NavBar from '../components/NavBar/NavBar.astro';

import '../styles/tailwind.css';

export interface Props {
    title: string;
    description: string;
    permalink: string;
    current?: string;
    opengraph?: string;
    opengraphAlt?: string;
    darkThemeOnly?: boolean;
}

const { title, description, permalink, opengraph = '', opengraphAlt = '', darkThemeOnly = false } = Astro.props;
---

<html lang="en" class={darkThemeOnly ? 'dark-theme-only' : ''}>
    <Head>
        <BaseHead
            title={title}
            description={description}
            permalink={permalink}
            opengraph={opengraph}
            opengraphAlt={opengraphAlt}
            darkThemeOnly={darkThemeOnly}
        />
    </Head>

    <body class="grid overflow-x-hidden h-lvh">
        <NavBar />

        <main>
            <slot />
        </main>

        <style is:global lang="scss">
            @import '../scss/settings';
            @import '../scss/fonts';
            @import '../scss/typography';
        </style>

        <style lang="scss">
            body {
                grid-template-rows: [navbar-start] var(--navbar-height) [content-start] auto;
            }
        </style>
    </body>
</html>
