---
import { Head } from 'astro-capo';
import BaseHead from '../components/BaseHead.astro';
import HeadingLinks from '../components/HeadingLinks.astro';
import TOC from '../components/TOC.astro';
import type { SEOMetadata } from '../types';

import '../styles/mdx-content.css';
import '../styles/tailwind.css';
import '../styles/typography.css';

type Props = SEOMetadata;

const { title, description, permalink, showTOC = true } = Astro.props;
---

<html lang="en">
    <Head>
        <BaseHead title={title} description={description} permalink={permalink} />
    </Head>

    <body class="mx-8 overflow-x-hidden bg-bg pb-24 text-[11pt] text-text antialiased dark:bg-bg-dark dark:text-text-dark">
        <main>
            <slot />
        </main>

        {
            showTOC && (
                <>
                    <TOC />
                    <HeadingLinks />
                </>
            )
        }
    </body>
</html>
